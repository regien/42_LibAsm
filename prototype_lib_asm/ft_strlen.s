;------------------------------------------------------------------
; Currently not using not a .section data to declare variables
; Neither using defines,
; todo: use it
;------------------------------------------------------------------
;default		rel
global			_ft_strlen

.section	data
_ft_strlen:
	mov		rax, 0 ; initializing value of rax to 0 (to use it as a counter)
	cmp		
	ret
